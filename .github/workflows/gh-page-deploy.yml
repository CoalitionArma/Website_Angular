# name: Deploy to GitHub Pages

# on:
#   # Runs on pushes targeting the specified branches
#   push:
#     branches: 
#       - development
#       - dev-pipeline-test
#   # Allows manual run from the Actions tab
#   workflow_dispatch:

# # Sets permissions for the GITHUB_TOKEN
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # Allow only one concurrent deployment, without canceling in-progress runs
# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   # Build job
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Pages
#         uses: actions/configure-pages@v5

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install dependencies
#         run: npm install

#       - name: Build the Angular app
#         run: npm run build --prod

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: dist/coalition-website/browser # Ensure this path matches your build output directory

#   # Deployment job
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Configure Git user
#         run: |
#           git config --global user.email "you@example.com"
#           git config --global user.name "Your Name"

#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4




name: Deploy to GitHub Pages

on:
  push:
    branches:
      - development
      - dev-pipeline-test

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Build Angular app to prod
        run: npm install && npm run build --prod

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist/browser
          branch: gh-pages  
          token: ${{ secrets.GH_TOKEN }}